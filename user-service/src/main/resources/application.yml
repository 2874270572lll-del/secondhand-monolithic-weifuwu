# 本地开发配置（大部分配置已迁移到Nacos Config）
# 如果Nacos Config不可用，将使用以下配置作为默认值

spring:
  application:
    name: user-service

  # 本地开发数据库配置（生产环境从Nacos获取）
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/user_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: lilili2004

server:
  port: 8081

# 注意：以下配置可以被Nacos Config中的配置覆盖
# JPA、Jackson、OpenFeign、Sentinel等配置已迁移到Nacos Config
